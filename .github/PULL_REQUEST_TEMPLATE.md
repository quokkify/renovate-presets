## Summary

<!-- Briefly describe the change to the preset(s). -->

## Affected presets

<!-- List the preset files modified, e.g. presets/gradle/default.json -->

## Validation

- [ ] `renovate-config-validator --strict` passes locally on all changed JSON files
- [ ] No private values introduced (no internal usernames, hostnames, registry URLs)
- [ ] Description field updated where relevant

## Backwards compatibility

<!-- Will existing consumers (repos that extend this preset) be affected? Describe migration if applicable. -->
