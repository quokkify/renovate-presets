name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: quokkify/project-toolkit/.github/workflows/release-please.yml@c79a1d53e620df2c20f8efd43d7488643d85e991 # v2.12.1
    with:
      mode: single
      release-type: simple
