name: Release Please

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: quokkify/project-toolkit/.github/workflows/release-please.yml@v2.2.0
    with:
      mode: single
      release-type: simple
