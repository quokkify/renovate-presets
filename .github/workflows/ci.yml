name: CI

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: renovate-config-validator
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: quokkify/project-toolkit/actions/setup-node@403467a8e9e5518dfa641c538c857af789a7a84e # v2.8.2
        with:
          node-version: "24"
          install-dependencies: "false"
          cache-dependencies: "false"

      - name: Validate all preset JSON files
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(find . -type f -name "*.json" \
            -not -name "package*.json")
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No preset JSON files found"
            exit 1
          fi
          echo "Validating ${#files[@]} files:"
          printf '  %s\n' "${files[@]}"
          npx --yes --package renovate -- renovate-config-validator --strict "${files[@]}"
