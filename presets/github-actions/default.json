{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Composable preset for GitHub Actions workflows. Updates each action separately on Monday mornings; non-major updates follow the base stability gate and major updates remain manual.",
  "extends": [
    "github>quokkify/renovate-presets//presets/base"
  ],
  "packageRules": [
    {
      "description": "Update each GitHub Action in a separate PR",
      "matchManagers": ["github-actions"],
      "automerge": true,
      "schedule": ["before 6am on Monday"]
    }
  ]
}
