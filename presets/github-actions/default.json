{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Composable preset for GitHub Actions workflows. Updates each action separately; does not restrict enabledManagers so it can be combined with other language presets.",
  "extends": [
    "github>ylazakovich/renovate-config//presets/base"
  ],
  "packageRules": [
    {
      "description": "Update each GitHub Action in a separate PR, overriding inherited grouping",
      "matchManagers": ["github-actions"],
      "groupName": null,
      "automerge": true,
      "schedule": ["before 6am on Monday"]
    }
  ]
}
