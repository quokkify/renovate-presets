{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Composable preset for Dockerfile and docker-compose. Adds grouping packageRules; does not restrict enabledManagers so it can be combined with other language presets.",
  "extends": [
    "github>ylazakovich/renovate-config//presets/base"
  ],
  "packageRules": [
    {
      "description": "Group Docker base image updates",
      "matchManagers": ["dockerfile", "docker-compose"],
      "matchUpdateTypes": ["minor", "patch", "digest"],
      "groupName": "docker images"
    }
  ]
}
