{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Service preset for Java/Gradle projects with Docker, docker-compose and GitHub Actions support. Extends gradle/default.",
  "extends": [
    "github>ylazakovich/renovate-config//presets/gradle/default"
  ],
  "enabledManagers": [
    "gradle",
    "gradle-wrapper",
    "docker-compose",
    "dockerfile",
    "github-actions",
    "custom.regex"
  ]
}
